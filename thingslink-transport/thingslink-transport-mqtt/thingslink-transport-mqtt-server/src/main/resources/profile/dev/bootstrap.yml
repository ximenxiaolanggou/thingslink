server:
  port: 8081
  servlet:
    context-path: /transport-mqtt
spring:
  application:
    name: Thingslink-Transport-Mqtt
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
        namespace: thingslink-dev
        server-addr: 127.0.0.1:8848
      config:
        username: nacos
        password: nacos
        namespace: thingslink-dev
        server-addr: 127.0.0.1:8848
        # 配置文件格式
        file-extension: yml
  main:
    allow-circular-references: true

